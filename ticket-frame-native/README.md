# Ticket Frame native app

This is the standalone Expo/iPhone entry for Ticket Frame. It does not load or expose the FlipFinderAI property interface.

Run `npm install`, then `npx expo start --tunnel`. Scan the QR code with the iPhone camera and open it in Expo Go.
