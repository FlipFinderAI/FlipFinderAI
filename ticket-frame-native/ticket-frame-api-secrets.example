# Copy these fields to .ticket-frame-api-secrets and enter the provider keys.
# The local file is ignored by source control and is never bundled into the app.

FOOTBALL_DATA_ORG_KEY=
API_FOOTBALL_KEY=
HIGHLIGHTLY_API_KEY=
